<div class="form-group">
    <label for="name">{{$label}}</label>
    <input type="{{$type}}" name="{{$name}}" id="" class="form-control" value="{{old($name)}}" placeholder="{{$placeholder}}">
  </div>
